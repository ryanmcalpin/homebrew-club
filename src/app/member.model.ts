export class Member {
  constructor(public firstName: string, public lastName: string, public dateJoined: string, public yearStarted: number, public bio: string, public favoriteStyles: string, public city: string, public state: string, public country: string, public zip: string) {}
}
